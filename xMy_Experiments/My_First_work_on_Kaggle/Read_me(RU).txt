Цель:
Прогнозируйте дефекты в программах на языке C, учитывая различные атрибуты кода.

Для каждого идентификатора в тестовом наборе я должен предсказать вероятность дефектов целевой переменной.

Но я не правильно понял цель задания
и на выходе моя модель просто выдавала двоичное значение для классов вместо вероятности.
Эта версия уже исправлена в последней строчке моего кода, но на кагле я не успел это сделать,
потому что я загрузил решение в самый последний момент и не успел его там поправить, о чем очень сожалею,
потому что мне было бы интересно посмотреть, как мой подход к выбору фич повлияет на конечный результат.


В любом случае:

Набор данных был создан на основе модели глубокого обучения, обученной на наборе данных о дефектах программного обеспечения.

train.csv — набор обучающих данных; defects — это двоичная цель, которая рассматривается как логическое значение (False=0, True=1).

test.csv — тестовый набор данных; моя цель — предсказать вероятность положительных дефектов (т. е. дефекты = True)